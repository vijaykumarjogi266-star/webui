# Runtime data — contains the encryption master key + user secrets. NEVER commit.
ai-workspace/data/

# Node / OS noise
node_modules/
*.log
.DS_Store
tests/

# local env overrides — commit .env.example, never .env
.env
.env.local
.env.*.local

# deploy scratch on VMs
*.pid
